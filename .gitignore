# Local .terraform directories
**/.terraform/*

# Terraform state files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Variable files (if sensitive or environment-specific)
# Uncomment or modify as needed based on your workflow
# *.tfvars
# *.tfvars.json

# Override files (local overrides not intended for version control)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform CLI configuration files
.terraformrc
terraform.rc

# Saved plan files (if using -out with terraform plan)
*.tfplan

# Sensitive files (e.g., encryption keys, if stored locally)
*.pem
*.key
*.pub
.terraform.lock.hcl